---
type: approval-ledger
updated: 2026-06-12
gates:
  - id: t0-merge
    name: T0 verified 合并补充
    streak: 0
    threshold: 10
    state: counting
    note: verified 数值/调查数据合并；门槛最低
  - id: newnode
    name: 新建节点
    streak: 2
    threshold: 20
    state: counting
    note: 连续 20 次批准零驳回进白名单
  - id: xedge
    name: 跨域边（grounds 族）
    streak: 1
    threshold: 20
    state: counting
    note: 理论→实践奠基边，观察期
  - id: retire
    name: 退役触发（T1/T2 当前态改变）
    streak: 0
    threshold: 0
    state: locked
    note: 设计决定：永久人工，不开放自动批准
  - id: disputed
    name: fact-check 分歧
    streak: 0
    threshold: 0
    state: locked
    note: 数值分歧逐例裁决，永久人工
---

# 审批权限

> **连续批准升级 · 一次驳回降级。** 每类高危写入一条进度：连续 `threshold` 次人工批准且零驳回 → `state: auto`
> （无人值守可直写，但仍记账）；**一次驳回 → streak 清零**，已 auto 的降回 counting。
> `state: locked` 的永久人工。记账规则：批准 streak+1 / 驳回 streak=0 / contested 不加不清（原地记一笔）。

## 记账操作（裁决时由 Agent 执行）

1. 翻转 `08-Ops/review/` 候选卡的 `status` 与 `decided` 字段。
2. 更新本页 frontmatter 对应 gate 的 `streak`（达到 threshold 时把 `state` 改为 `auto`；驳回清零并降级）。
3. 在下方审计日志 **append 一行**（永不改旧行）。

## 审计日志（append-only）

| 日期 | gate | 对象 | 裁决 | streak 变化 | 备注 |
|---|---|---|---|---|---|
| 2026-06-11 | — | （账本建立） | — | 全部清零起算 | 陋居层落地 |
| 2026-06-12 | newnode | 理论沿革深挖批量 16 节点（ethics 11 + metaphysics 5） | approved | 0→1 | 交互会话，用户明确指令「充分挖掘理论沿革」即裁决；引用纪律同步生效 |
| 2026-06-12 | newnode | 三部曲补全批量 13 节点（《背叛》《天幕红尘》：ethics 8 + epistemology 5） | approved | 1→2 | 交互会话，用户明确指令「继续编译另外两部小说」即裁决；认识论域首批节点 |
| 2026-06-12 | xedge | 见路不走 →grounds→ 得救之道（认识论→伦理学） | approved | 0→1 | 文本依据充分（觉悟的操作界面，第十四章）；交互会话随编译指令裁决 |
